---
"hex-app": patch
---

Keep audio when transcription fails: failed recordings are now saved to history with a Retry button instead of being left in the purgeable temp directory (or deleted outright, in the case of append recordings). Recordings stranded by a crash are recovered on next launch.
